broker: alpaca
environment: paper      # paper = Demo-Konto (empfohlen), live = echtes Konto
instrument: AAPL        # Alpaca-Notation: Ticker-Symbol, kein Slash/Underscore
granularity: H1         # Codes: M1,M5,M15,M30,H1,H4,D,W

strategy:
  fast_ma: 20
  slow_ma: 50
  rsi_period: 14
  rsi_buy_max: 65   # RSI muss unter diesem Wert liegen, damit ein Long-Signal bestaetigt wird
  rsi_sell_min: 35  # RSI muss ueber diesem Wert liegen, damit ein Exit-Signal bestaetigt wird
  atr_period: 14    # Average True Range: misst tatsaechliche Volatilitaet fuer Stop/Target

risk:
  initial_capital: 10000
  risk_per_trade: 0.01          # 1% des Kapitals pro Trade riskieren
  stop_loss_atr_mult: 2.0       # Stop-Loss = Entry - 2x ATR (skaliert mit Volatilitaet)
  take_profit_atr_mult: 4.0     # Take-Profit = Entry + 4x ATR (2:1 Chance-Risiko wie zuvor)
  fee_pct: 0.0002               # Naeherung fuer Transaktionskosten

paper_trading:
  poll_interval_seconds: 60
